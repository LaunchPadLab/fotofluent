/* eslint-disable no-unused-vars */

import Vue from 'vue'
import Options from '../component/options.vue'

const app = new Vue({
  el: '#app',
  render (h) {
    return (
      <Options />
    )
  }
})
